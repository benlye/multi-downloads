name: Deploy

on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '0 */4 * * *'

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run Generate-MultiDownloadPage.ps1
      run: .\build\Generate-MultiDownloadPage.ps1
      shell: pwsh
      
